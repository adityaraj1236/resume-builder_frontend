// Shared visual constants for ResumeContentEditor - one place to tune the accent
// color/spacing so every section's list/entry/field components stay visually
// consistent (Teal-style: borderless rows, one accent color, tight spacing)
// without each file hardcoding its own copy of the same values.
export const editorTheme = {
  accent: "#2563eb",
  accentSoft: "#f0fdfa",
  border: "#e5e7eb",
  textMuted: "#6b7280",
  textFaint: "#9ca3af",
  danger: "#dc2626",
  rowRadius: 8,
};
